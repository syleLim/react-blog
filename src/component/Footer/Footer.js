import React			from "react"
import { Record }		from "immutable"

import { FooterStyle, FooterContent }	from "../../styles"

const Footer = ({}) => {

	return (
		<FooterStyle>
			<FooterContent>footer</FooterContent>
		</FooterStyle>
	);
}

export default Footer;